spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/sentinel-dev
      database: sentinel-dev
  kafka:
    bootstrap-servers: localhost:9092
  redis:
    host: localhost
    port: 6379

sentinel:
  embedding:
    api-key: ${OPENAI_API_KEY:sk-dev-placeholder}

logging:
  level:
    com.sentinel: DEBUG
